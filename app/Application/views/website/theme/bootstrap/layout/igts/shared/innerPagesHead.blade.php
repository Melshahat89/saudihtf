<section class="sec_head {{ (isMobile()) ? '' : 'sticky-stopper' }}">
    <div class="wrapper">
        <section class="title mblg">
            <h2 class="text_capitalize">{{$title}}</h2>
            <p style="color: white;">{{isset($subTitle) ? $subTitle : ''}}</p>
        </section>
    </div>
</section>